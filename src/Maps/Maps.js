import React from 'react';
import './Maps.css';

const Maps = (props) => {
    return (
        <section
            className='Maps__outer-container'
        >
            <h2>Maps</h2>
            <p>Maps coming soon!</p>
        </section>
    );
}

export default Maps;